name: Sunset Monitor

on:
  workflow_dispatch:

jobs:
  test-api:
    runs-on: ubuntu-latest

    steps:
      - name: Test sunsetbot API
        run: |
          curl -v --max-time 30 \
            "https://sunsetbot.top/?query_id=123456&intend=select_city&query_city=%E5%AE%81%E6%B3%A2&event_date=None&event=set_1&times=None"
